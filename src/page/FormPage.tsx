import { FormContainer } from "@/components/Form";

const FormPage = () => {
  return (
    <div className="w-full h-screen flex items-center justify-center mt-20 md:mt-0">
      <FormContainer />
    </div>
  );
};

export default FormPage;
